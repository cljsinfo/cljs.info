===== Name
cljs.core/count

===== Signature
[x]

===== Description

Returns the number of items in `x`.

`count` works on arrays, lists, maps, sets, strings, and vectors.

`(count nil)` returns 0.

===== Related
