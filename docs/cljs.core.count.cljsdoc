===== Function
cljs.core/count

===== Signature
[coll]

===== Description

Returns the number of items in the collection.

`(count nil)` returns 0.

Also works on strings, arrays, and maps.

===== Related
