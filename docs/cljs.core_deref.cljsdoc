===== Name
cljs.core/deref

===== Signature
[o]

===== Description

Returns the current value of atom `o`.

The `@` reader macro is often used instead of `deref`. `@foo` is the same thing
as `(deref foo)`.

===== Related
cljs.core/atom
